{% extends 'admin_base.html.twig' %}

{% block title %}Nouvelle référence{% endblock %}

{% block body %}
    <div class="section-uk-section">
        <div class="uk-container">
            <h1>Créer une nouvelle référence</h1>

            {{ include('admin/reference/_form.html.twig') }}
            <div class="uk-margin-bottom uk-margin-top">
                <a class="uk-button uk-button-link" href="{{ path('reference_index') }}">Retourner à la liste des Références</a>
            </div>

        </div>
    </div>
{% endblock %}