<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/uikit/3.1.5/css/uikit.min.css" />
    <link rel="stylesheet" href="style_ljm.css">
    <title>emails</title>
</head>

<body>
    {% block body %}
    <div class="uk-container">
        <div class="uk-card uk-card-default uk-width-1-2@m">
            <div class="uk-card-header">
                <div class="uk-grid-small uk-flex-middle" uk-grid>
                    <div class="uk-width-auto">
                        <img src="{{absolute_url(asset('img/Logos_ljm/Logo_Hpc_1.png'))}}" id="logo" alt="Agence-Ljm">
                    </div>
                    <div class="uk-width-expand">
                        <h3 class="uk-card-title uk-margin-remove-bottom">Confirmation de demande {{ value.subject }}
                        </h3>
                        <p class="uk-text-meta uk-margin-remove-top"> le {{ "now"|date("m/d/Y") }}</time></p>
                    </div>
                </div>
            </div>
            <div class="uk-card-body">
                <p>Bonjour {{ value.lastName }} {{ value.firstName }}</p>
                <p>Entreprise : {{ value.company }}</p>
                <p>Votre demande :</p>
                {{ value.content }}
            </div>
            <div class="uk-card-footer">
                <p class="uk-text-meta">Votre demande est prise en compte, nous y répondrons dans les meilleurs délais
                </p>
            </div>
        </div>
    </div>
    {% endblock %}
</body>

</html>